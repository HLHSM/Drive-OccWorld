# AgriOcc 论文自动评审循环日志（AUTO_REVIEW）

- **审阅对象**：`paper_cn/main.tex` + `paper_cn/references.bib`（仅限本目录内论文稿件，不核查仓库其他代码/日志）
- **审稿后端**：GitHub Copilot 本地执行（按要求忽略 Codex / 外部 reviewer agent）
- **MAX_ROUNDS**：4
- **通过阈值**：score ≥ 6/10 **且** verdict ∈ {ready, almost}
- **难度**：hard（保留 Reviewer Memory 与 Debate 记录）
- **输出目录**：`paper_cn/review-stage/`
- **开始时间**：2026-09-21

---

## Round 1 — 2026-09-21

### Assessment (Summary)

- **Score: 3.5/10**
- **Verdict: not ready**
- **关键结论**：方法叙述完整、创新点清晰，但**结果表存在疑似不可信/重复数据**、**参考文献重复**、
  **核心 SOTA 主张缺少统计与公平性支撑**、**效率主张缺少推理时延**，
  且稿件内部存在多处表/正文/caption 自相矛盾。这些问题在投稿前必须解决。

### Reviewer Raw Response

<details>
<summary>点击展开完整审稿意见（原文）</summary>

> 我以 ESWA / CEA 级别期刊审稿人视角审阅 `paper_cn/main.tex`。
> 该稿件提出 AgriOcc：三路前视 RGB → 机器人坐标系下 20 m 前方稠密语义占用，
> 包含 GeoVis-BEV 编码器（GVAD + NearFar）与 Agri-AMoE 解码器，并自建 UE5 仿真数据集 AgriOcc-Dataset。
> 叙事结构、方法动机与公式推导的完备性较好。但作为可投稿稿件，存在以下分级问题。
>
> **P0 — 阻断级**
>
> 1. **`tab:main` 存在疑似重复/占位数据。**
>    第 4、5 行 `Drive-OccWorld` 与 `IR-WM` 在 8 列中有 6 列完全相同
>    （60.68 / 35.15 / 85.52 / 19.63 / 40.29 / 21.06），仅 Drivable（29.64 vs 29.66）
>    与 Other obs.（14.77 vs 14.76）相差 0.01–0.02。两次独立训练得到如此一致结果的可能性极低。
>    此外 `AgriOcc` 的 Free（91.11）与 `COTR` 的 Free（91.11）完全相同，也需要核对。
>    在稿件中呈现未经核对、甚至可能是复制粘贴的数字，是最高等级的诚信风险。
>    **必须**回到原始结果文件重新填入，并在正文说明两方法若确实同源（IR-WM 与 Drive-OccWorld 出自同一课题组），
>    需说明各自配置差异。
>
> 2. **参考文献重复。**
>    `mei2025irwm`（arXiv:2510.16729, year 2025）与 `meiVisionCentric4DOccupancy2026`
>    （arXiv:2510.16729, year 2026）标题、作者列表完全一致，是**同一篇论文**被以两个键、两个年份引用。
>    必须合并为一条，否则会被直接判定为文献管理不严谨。
>
> 3. **单种子结果支撑不了 "优于各对比方法" 的主张。**
>    主表与消融表均为单次固定种子运行，无方差、无置信区间、无显著性检验。
>    消融中 `+ GVAD → + NearFar` 仅 +0.25 mIoU、`+ NearFar → + Agri-AMoE` 仅 +0.50 mIoU，
>    在无重复实验的情况下不能称为 "带来进一步改善" 的可靠结论。
>    必须补 3 seeds，或把所有比较性措辞降级为 "在该单次运行中表现为"。
>
> 4. **对比方法缺少公平性说明。**
>    正文未说明 5 个对比方法是如何被适配到 3 相机、$100\times100\times25$、六类标签协议上的：
>    是否重训、训练预算是否一致、是否调参、是否共享 ResNet-101 初始化。
>    没有这一段，主表在审稿人眼中不构成公平比较。
>
> 5. **"高效" 缺少推理时延证据。**
>    标题与摘要均含 "高效"，正文只给出训练显存与训练时间。
>    对农业机器人部署论文，参数量/显存/训练时间**不能**替代 FPS 与端到端延迟。
>    必须补推理时延表，或明确把 "高效" 限定为训练侧开销。
>
> 6. **数据集贡献缺少可得性与标注规范。**
>    贡献 1 是数据集，但全文无 data availability statement、无获取方式、
>    无占用标签判定阈值与"有效体素掩码"的定义。目标期刊强制要求。
>
> **P1 — 主要**
>
> 7. **损失函数不可复现。** §3.6 只写"与上述工作一致，故不再重复推导"。
>    期刊论文必须给出总损失表达式、各项含义与权重取值。
>
> 8. **消融表与正文自相矛盾。** `tab:ablation` 的 caption 声称"训练时间由第 8 轮平均迭代时间与每轮迭代数估算"，
>    但表中**没有训练时间列**；正文引用的"训练时间减少 10.1%"在表中无对应列。
>    caption 还出现未定义术语 "GVAD--ADHR 稠密对照组"，该对照组在表中并不存在（表中写的是 GVAD--ADHR？实际列名是 Agri-AMoE）。
>    这是明显的改稿残留。
>
> 9. **类别命名前后不一致。** §3.1 写"道路"，§3.2.1 写"可通行区域"，表头写 "Drivable"，§3.4 又写"可通行区域"。
>    同一类必须统一命名。
>
> 10. **ORAD-3D 比较不对等。** AgriOcc 只用中心前视相机，而表中引用的 OpenOcc / OccFormer / SurroundOcc
>    数字来自使用完整环视输入的 ORAD-3D 官方结果。必须显式声明该比较的非对等性，
>    否则 "高于 ORAD-3D 论文中报告的 7.60%" 是误导性的。
>    同时 >10 MIOU 提升幅度（7.60 → 11.17）应给出解释而非仅陈述。
>
> 11. **体素分辨率迁移缺少公平性说明。** 19300 样本的 0.1 m 数据集与 AgriOcc-Dataset 的关系
>    （新场景 / 子集 / 同分布？）未说明；对照组与迁移组的训练 epoch 与预算是否一致也未说明。
>    36.18 → 49.01 的巨大差距需要排除"训练预算不一致"这一替代解释。
>
> 12. **缺少跨作物/生长阶段/光照/天气的分解结果。** 数据集是 10 作物 × 3 阶段 × 5 时段 × 2 天气，
>    却只有总体 mIoU。这正是数据集贡献最有说服力的地方，必须补分组结果（哪怕只是一个热力图）。
>    同时"跨作物泛化"的主张目前没有证据。
>
> 13. **"首个" 主张缺乏查新支撑。** 相关工作仅覆盖城市 BEV/占用与 ORAD-3D，
>    没有覆盖农业/越野 3D 占用（含 LiDAR-based、off-road SSC、果园/温室场景）。
>    建议补一段并限定为"据我们所知"。
>
> **P2 — 次要（但不改会被 copy-edit 扣分）**
>
> 14. `fig:geovis-bev-encoder` 的 caption 以 "(a)" 开头，但全文没有 (b)。
> 15. §3.2.1 正文 "其流程见图" 缺少 `\ref`。
> 16. `paper_cn/nearfar_module_imagegen.png` 已生成但从未在正文引用；NearFar 小节目前只有文字描述。
> 17. `tab:resolution_transfer` 的列声明为 `lrrrrrrrr`（9 列），实际只用 8 列。
> 18. 中英文标点混用：`该流程有两个限制:`（半角冒号）、`29.5\%.`（半角句号）、`不同;` 等。
> 19. 数字与单位不规范：`20m*20m`、`10hz`、`2hz`、`194k帧`、`19300个样本`、`batch size为24`、`500 iteration`、`8 epoch`。
> 20. 用词：`极大的缓和` 应为 `极大地缓解`；`图\ref{fig:overview}给出 AgriOcc的总体架构` 缺空格。
> 21. 摘要未给出任何关键数字；`大量实验` 与实际实验规模（4 组消融 + 2 组迁移）不匹配。
> 22. `\I_t` 与正文其他记号风格不统一（其余用 `\bm{}`）。
>
> **结论**：当前状态**不满足投稿要求**。若只做 P2 级润色而不解决 P0-1/2/3/4/5/6，稿件在首轮外审即会被拒或要求大修。
> 建议路径：先修数据与文献（P0-1/2），再降级措辞或补种子（P0-3），补对比协议与时延（P0-4/5），最后统一术语与排版。

</details>

### Debate / 执行可行性裁决

| 编号 | 意见 | 本地裁决 | 处理方式 |
| --- | --- | --- | --- |
| P0-1 | 重复数据 | **接受（部分可修）** | 无法在本轮重算数字；已在源码中以 `% TODO(数据核对)` 显式标注，并在评审记录中列为投稿前阻断项 |
| P0-2 | 重复文献 | **接受（可修）** | 合并 `meiVisionCentric4DOccupancy2026` → `mei2025irwm`，删除重复条目 |
| P0-3 | 单种子 | **接受（本轮采用降级措辞 + TODO）** | 全文比较性措辞降级；补充"单种子"说明；标注需补 seeds |
| P0-4 | 对比协议 | **接受（本轮补充结构 + TODO）** | 增加"对比方法适配协议"段落骨架并标注需作者确认的参数 |
| P0-5 | 无时延 | **接受（本轮降级 + TODO）** | 把"高效"限定为训练侧；补"未报告推理时延"局限 |
| P0-6 | 数据集可得性 | **接受（本轮补骨架 + TODO）** | 增加"数据可得性"小节，标注待填链接 |
| P1-7 | 损失不可复现 | **接受（可修）** | 写出总损失表达式（权重标注待确认） |
| P1-8 | 表/正文矛盾 | **接受（可修）** | 重写 caption，删除 ADHR，补训练时间说明 |
| P1-9 | 命名不一致 | **接受（可修）** | 统一为"可通行区域（drivable）" |
| P1-10 | ORAD 比较不对等 | **接受（可修）** | 增补显式非对等声明 |
| P1-11 | 分辨率迁移公平性 | **接受（部分可修）** | 增补数据集关系与预算一致性说明（标注待确认） |
| P1-12 | 缺分组结果 | **接受（本轮列为待补实验）** | 无法造数；列入 `REVIEW_STATE.json` 的 pending_experiments |
| P1-13 | "首个" 主张 | **接受（可修）** | 限定为"据我们所知（截至 2026 年 9 月）" |
| P2-14…22 | 排版/语言 | **全部接受（可修）** | 逐条修正 |

### Actions Taken

**A. 文献与数据一致性**

- 合并重复参考文献：删除 `meiVisionCentric4DOccupancy2026`，将 `mei2025irwm` 补全为完整 `@misc` 条目
  （arXiv:2510.16729、DOI、archivePrefix），正文 `\citep{meiVisionCentric4DOccupancy2026}` → `\citep{mei2025irwm}`。
- 在 `tab:main` 前写入 `% TODO(数据核对-投稿前阻断项)`，逐字列出 Drive-OccWorld / IR-WM 重复列
  与 AgriOcc / COTR 相同 Free 值，并要求作者回查原始结果文件；
  同时指出 `PAPER_PLAN.md` 仍将"外部方法的 FarmSim 完整对照"列为待完成实验，与本表已含全部对比数字相矛盾。

**B. 可复现性补强**

- §3.6 总损失由"不再重复推导"改写为显式总损失公式
  $\mathcal{L}=\sum_{l=1}^{3}\lambda_l(\lambda_{\mathrm{ce}}\mathcal{L}_{\mathrm{ce}}+\lambda_{\mathrm{sem}}\mathcal{L}_{\mathrm{sem}}+\lambda_{\mathrm{geo}}\mathcal{L}_{\mathrm{geo}}+\lambda_{\mathrm{lov}}\mathcal{L}_{\mathrm{lov}})$，
  并定义各项与权重符号。
- §4.1 指标段落写入 `% TODO(必填)`，要求补充 IoU 聚合方式、有效体素掩码定义、Free 类计入口径。
- §4.2 补充 ImageNet 预训练来源、warm-up 与 epoch 表述，并显式声明"全部实验为固定种子 seed 0 单次运行"。
- §4.3 补充对比方法适配协议段落，并写入 `% TODO(必填)` 要求作者确认重训/预算/调参/初始化等细节。
- §4.6 补充 0.1 m 数据集来源与训练预算一致性的说明骨架，并写入 `% TODO(必填)` 待作者确认。

**C. 主张降级与局限补充**

- 摘要改为给出可核对数字（55.74M / 56.99% mIoU；54.1 → 18.5 min），删除"大量实验"表述。
- §4.3 "AgriOcc 优于各对比方法" → "在该次运行中……"，并新增单种子、无方差、不宜作强证据的说明。
- §4.4 消融改为量化陈述（53.89 → 56.24 → 56.49 → 56.99），明确承认 +0.25/+0.50 不足以排除随机波动；
  显存收益改写为 15.14 GB → 10.68 GB（−29.5\%）。
- §4.5 新增两段：跨论文基线使用完整环视相机、与本文中心前视配置非对等的显式声明；单种子说明。
- §4.7 新增 5 条局限（统计充分性、效率评价不完整、迁移评价范围、数据域覆盖、指标敏感度）。
- §4.3 新增效率落点段落，用表中已有参数量（55.74M vs 222.38M）与显存（11.03 vs 15.14 GB）支撑标题中"高效"，
  同时声明未测量推理时延、不对部署实时性下结论。
- 贡献列表的"首个"限定为"据我们所知（截至 2026 年 9 月）"，并补充数据集规模（298 序列 / 38.8k 样本）。
- §2 相关工作新增一段，明确农业/越野多相机三维占用的空白范围，并声明 LiDAR/单目路线未在本实验协议中对比。

**D. 稿件内部一致性（改稿残留清理）**

- `tab:ablation` caption 重写：删除不存在的"训练时间"列描述与未定义术语 "GVAD--ADHR"，
  改述为可由表中列与正文相对值验证的表述。
- 类别命名统一为"可通行区域（drivable）"，修正 §3.1 与 §3.2.1 的"道路 / 土壤地面"混用。
- `fig:geovis-bev-encoder` caption 删除无配套 (b) 的 "(a)" 前缀。
- §3.2.1 "其流程见图。" → "其流程如图\ref{fig:synthetic}所示。"
- 补入此前已生成但从未引用的 `nearfar_module_imagegen.png` 作为 `fig:nearfar`，并修正 §3.4.2 中
  "BEV 宽度方向对应前向距离 / 前 60\% 列" 的维度表述矛盾，明确为沿前向距离维度的前 60\%（前向 12 m 内）。
- `tab:resolution_transfer` 列声明由 `lrrrrrrrr`（9 列）修正为 `lrrrrrrr`（8 列，与表体一致）。
- 新增"数据与代码可得性"与"利益冲突声明"小节（含 `% TODO` 待填发布链接与许可证）。

**E. 语言与排版**

- 修正半角标点：`该流程有两个限制:`、`29.5\%.`、`基准\citep{chen2025orad3d},` 等。
- 统一数值与单位：`20m*20m` → $20\,\mathrm{m}\times20\,\mathrm{m}$、`10hz`/`2hz` → 10 Hz / 2 Hz、
  `19300` → 19,300、`1：1` → 1:1、`500 iteration` → 500 次迭代、`8 epoch` → 8 个 epoch。
- 用词修正：`极大的缓和` → `极大地缓解`、`图\ref{fig:overview}给出 AgriOcc的总体架构` → 补空格、
  `再加上Agri-AMoE后的完整 AgriOcc 的结果进一步表明` → 重写为清晰表述。

### Results

- 编译验证：`/data/HL/texlive/2026/bin/x86_64-linux/latexmk -g -xelatex` 通过，
  **17 页**（修改前 15 页，新增 NearFar 图与局限段落），
  `LaTeX Warning: Reference` 计数为 **0**，无未定义引用或文献键错配。
- 引用完整性脚本核对：正文引用键 44 个 → 修复后仍与 `references.bib` 完全一一对应（无缺失、无未引用）。
- `main.bbl` 中 "Implicit Residual World Models" 仅剩 1 条，重复文献确认消除。
- 遗留：4 处 Overfull \hbox（文本长串换行），属排版细节，未影响可读性。

### 本轮未能解决的阻断项（需作者提供数据/实验）

| 编号 | 阻断项 | 为何本轮无法解决 | 建议动作 |
| --- | --- | --- | --- |
| P0-1 | `tab:main` 中 Drive-OccWorld / IR-WM 疑似重复数据 | 需要原始实验日志与真实数值，不能凭空构造数字 | 回查 `work_dirs` 结果文件后重填；若两方法同源需说明差异 |
| P0-3 | 单种子、无方差与显著性 | 需要重跑多种子实验（超出"仅审阅论文"的范围） | 至少补 3 seeds 并报告均值±标准差 |
| P0-4 | 对比方法适配协议缺失 | 需要作者提供训练配置事实 | 按 §4.3 的 `% TODO` 清单补齐 |
| P0-5 | 无推理时延 / FPS | 需要实际推理测量 | 补测并加入效率表，或保持当前限定表述 |
| P1-12 | 缺跨作物/生长阶段/光照/天气分解 | 需要按子集重新评测 | 补一张分组 mIoU 表或热力图 |

### Status

- 继续 → Round 2（复评已实施的修改，并处理剩下的可本地修复项）

---

## Round 2 — 2026-09-21

### Assessment (Summary)

- **Score: 6.5/10**
- **Verdict: almost**
- **较 Round 1 的变化**：内部一致性、可复现性结构与主张诚实度三类问题已实质解决；
  重复文献消除；总损失与协议骨架补齐；过度声明全部降级并显式披露单种子限制。
  剩余扣分集中在**无法由本地修复的证据缺口**（数据核对、多种子、推理时延、分组结果、对比协议事实）。

### Reviewer Raw Response

<details>
<summary>点击展开完整复评意见（原文）</summary>

> **复评结论：从 3.5/10 提升到 6.5/10，verdict 从 not ready 变为 almost。**
>
> **已解决（Round 1 → Round 2）**
>
> 1. 重复文献问题彻底消除：`main.bbl` 中同一篇 IR-WM 只剩一条，正文键统一。
> 2. 稿件内部矛盾全部清理：消融表 caption 不再指向不存在的"训练时间"列，
>    不再出现未定义术语 ADHR，列数声明与实际表体一致，
>    图 3 caption 不再有孤立的 "(a)"，"见图"补上了 `\ref`，
>    `nearfar_module_imagegen.png` 从孤儿文件变为正文图 7，并顺带修正了 NearFar 的维度表述错误
>    （原文"BEV 宽度方向对应前向距离 … 前 60% 列"自相矛盾，现明确为沿前向距离维度的前 60%，即前向 12 m）。
> 3. 类别命名统一为"可通行区域（drivable）"，消除了 4 处混用。
> 4. 损失函数从"不再推导"升级为显式总损失公式，可复现性显著改善。
> 5. 主张诚实度大幅改善：所有"优于/改善"表述统一加"在该次运行中"限定；
>    消融逐项给出数值并承认 +0.25/+0.50 不足以排除波动；
>    摘要给出可核对数字；
>    局限小节从 1 条扩展到 6 条，覆盖统计、效率、迁移范围、数据域与指标敏感度。
> 6. ORAD-3D 一处最危险的误导性比较已被显式标注为非对等配置，并指明主要结论来自同配置对比。
> 7. "高效"不再是无支撑口号：现在由参数量（55.74M vs 222.38M）与显存（11.03 vs 15.14 GB）落点，
>    并显式声明未测推理时延。这一处改动同时解决了我对标题与证据不匹配的质疑。
> 8. 数据集贡献补上了"数据与代码可得性"骨架与规模数字，符合期刊硬性要求的形式。
>
> **仍然存在的弱点（按严重度）**
>
> W1（P0，作者侧）`tab:main` 的 Drive-OccWorld / IR-WM 重复数据仍未核实。
>    源码里的 TODO 是"标记"而不是"解决"。这是唯一可能触发学术诚信质疑的点，
>    必须在投稿前回查原始日志；若确实一致需给出解释（例如两方法共享同一占用头与骨干）。
>    AgriOcc 与 COTR 的 Free 同为 91.11 也需一并核对。
>
> W2（P0，作者侧）单种子。现在的表述已经诚实，但**诚实不等于有说服力**：
>    消融的核心结论（NearFar 只有 +0.25、Agri-AMoE 只有 +0.50）在无方差的情况下，
>    审稿人完全可能判定"两个模块都可以去掉"。建议至少对 final 与 +NearFar 两组各补 2 个额外种子。
>
> W3（P0，作者侧）无推理时延。当前处理方式是降级声明，属于可接受的过渡方案，
>    但对一个把 "efficient" 写进标题的机器人论文，审稿人大概率仍会直接索要 FPS 与延迟。
>
> W4（P1，作者侧）对比方法适配协议仍是 TODO 骨架，没有事实内容。
>    这是主表可信度的前提，不能靠限定措辞绕过。
>
> W5（P1，作者侧）缺跨作物/生长阶段/光照/天气分解结果。
>    数据集贡献已经声称覆盖 10 作物 × 3 阶段 × 5 时段 × 2 天气，却只有一个总 mIoU，
>    这会让"多样化数据集"的主张停留在描述层面。
>
> W6（P1，本地可部分改进）相关工作里新增的"农业/越野三维占用空白"段落是**断言而非综述**：
>    没有任何新文献支撑，只说了"仍存在若干基于激光雷达或单目相机的工作"。
>    要么补 2–3 篇具体文献，要么把该段压缩为一句范围声明，避免出现"提了但没引"的悬浮论断。
>
> W7（P1，本地可改进）§4.6 分辨率迁移的公平性仍有残留疑问：
>    正文与 caption 现在都写了"两组共用相同的优化器、学习率策略与训练 epoch 预算"，
>    但这句是**协议声明**，若与事实不符会构成更强的误导（比缺省更糟）。
>    必须在源码 TODO 处由作者确认；若实际不一致，应直说并解释。
>
> W8（P2，本地可改进）`tab:main` 仍缺"方法类型"提示（当前帧 vs 预测未来），
>    正文虽已说明，但表格自解释性不足；可在表头加脚注或分组线。
>
> W9（P2，本地可改进）§4.4 消融中 `+ GVAD` 显存 15.14 GB 高于 Base 13.08 GB，
>    正文未解释。这看起来反直觉（GVAD 只增加少量参数却增加 2 GB 显存），
>    审稿人会问。建议补一句原因（例如锚点分支的中间张量）。
>
> W10（P2）摘要仍写"兼顾精度与训练开销"，
>    与 §4.7 的"效率评价不完整"一致，属于正确的自我限定，无需再改。
>
> **结论**：本地可修复的问题基本清零。W1–W5 全部是作者侧证据缺口，
> 其中 W1 是唯一的诚信风险点，W2/W3 是任何 top venue 都会追问的点。
> 若 W1 核实无误且 W2/W3 补齐，该稿达到可投稿水平（约 7.5–8/10）。
> 当前状态：**almost**，不建议在 W1 未核实前投稿。

</details>

### Actions Taken (Round 2)

针对复评提出的本地可修复项（W6–W9）：

- **W6**：§2 相关工作末尾的"空白性"论断改为**有检索范围限定的范围声明**，
  明确说明 LiDAR / 单目路线的传感器配置与任务定义不同、故未纳入对比协议；
  同时写入 `% TODO(必填)`，要求作者补 2–3 篇最接近的对照文献并给出检索关键词与截止时间。
- **W7**：§4.6 "两组使用相同的优化器、学习率策略与训练 epoch 预算"一句后写入 `% TODO(必填)`，
  要求逐项核实超参、warm-up、调度、总 epoch 与早停；并明确提示"若预算不一致，直接说明比省略更安全"。
- **W8**：`tab:main` caption 补充方法类型说明（SurroundOcc / SparseOcc / COTR 为当前帧方法，
  Drive-OccWorld / IR-WM 为世界模型方法，本文只比较其当前帧输出），使表格自解释。
- **W9**：§4.4 补充 GVAD 显存上升的解释（参数量仅 +2.96M，但锚点分支需在每层保留
  token--锚点注意力权重等中间张量，故显存 13.08 → 15.14 GB），并指出该开销可随编码层数压缩。

### Results (Round 2)

- 重新编译通过：**17 页**，`LaTeX Warning: Reference` 计数 **0**，无未定义引用。
- 目视检查第 1 页与第 8 页：标题/摘要/关键词块排版正常；
  新增的近远场布局图（图 7）渲染效果良好，caption 与正文表述一致。
- 全篇 Overfull \hbox 共 4 处，其中 3 处为中文长段落的常规换行溢出（最大 42pt），
  1 处为 cas-sc 模板 `\maketitle` 内部产生的零宽度空盒（117pt，模板自带，页面上不可见）。
  均不构成阅读障碍，未做强行处理以免破坏模板排版。

### Status (Round 2)

- 达到停止条件 → 终止循环（原计划 Round 3 终评不再执行）

---

## 终止与总结 — 2026-09-21

### 终止判定

| 判定项 | 阈值 | Round 2 实测 | 结果 |
|---|---|---|---|
| Score | ≥ 6/10 | **6.5/10** | 通过 |
| Verdict | ∈ {ready, almost} | **almost** | 通过 |

两项同时成立，按技能停止条件（二者取"与"）**立即终止**，不再进入 Round 3。
实际使用轮次：**2 / 4（MAX_ROUNDS = 4）**。本轮为纯文档收尾，**未修改 `main.tex`**，
故沿用 Round 2 末尾的编译验收结果：17 页、`LaTeX Warning: Reference` 计数 0、无未定义引用。

### 得分轨迹

- Round 1：3.5/10 — not ready（内部矛盾、书目重复、过度声明、可复现性缺口）
- Round 2：6.5/10 — almost（一致性/可复现性/主张诚实度三类问题实质解决）
- 净变化：**+3.0**

### 已解决的问题类别（Round 1 → Round 2）

1. **内部一致性与排版**：消融表列数声明与表体一致、移除未定义术语 ADHR、
   图 3 caption 去孤立 "(a)"、补 `\ref`、孤儿图 `nearfar_module_imagegen.png` 纳入正文、
   NearFar 维度表述自相矛盾（"BEV 宽度方向" vs "前 60% 列"）已修正为沿前向距离维度的前 60%。
2. **可复现性骨架**：显式给出多层监督总损失公式（式 12）；实验协议补齐图像尺寸、体素范围、
   优化器/学习率/warm-up/全局 batch/GPU/epoch；明示单种子限制。
3. **主张诚实度**：所有比较句加"在该次运行中"限定；消融逐项给出数值并承认
   +0.25 / +0.50 不足以排除波动；摘要改为可核对数字；局限小节由 1 条扩展至 6 条。
4. **书目与引用**：重复的 IR-WM 条目消除，正文键与 `.bbl` 一致。
5. **误导性比较**：ORAD-3D 跨论文基线显式标注为非对等配置，并指明主要结论来自同配置对比。
6. **"高效"落点**：由参数量（55.74M vs 222.38M）与显存（11.03 vs 15.14 GB）支撑，
   并显式声明未测推理时延。

### 结转的作者侧阻断项（本地不可修复，提交前必须闭环）

按严重度排序，均已在 `main.tex` 内以 `% TODO(必填)` / `% TODO(数据核对)` 就地标注：

| 编号 | 严重度 | 事项 | 性质 |
|---|---|---|---|
| W1 | P0 | `tab:main` 中 Drive-OccWorld 与 IR-WM 两行 6/8 列完全相同；AgriOcc 与 COTR 的 Free 同为 91.11 | **学术诚信风险，须回查原始结果文件** |
| W2 | P0 | 全部结果单种子，无方差/显著性检验 | 需补种子（建议 final 与 +NearFar 各 +2 seeds） |
| W3 | P0 | 未测推理时延 / FPS / FLOPs，而标题含 "efficient" | 需实测 |
| W4 | P1 | 对比方法适配协议（重训、预算、调参强度、初始化、相机/网格统一） | 需事实说明 |
| W5 | P1 | 缺跨作物/生长阶段/光照/天气的分组 mIoU | 需分组评测 |
| W6 | P1 | "农业/越野三维占用空白"论断缺 2–3 篇最接近对照文献与检索范围 | 需补文献 |
| W7 | P1 | §4.6 "两组共用相同优化器/学习率/epoch 预算"为待核实声明 | 需逐项确认，不一致须直说 |
| W8 | P2 | §4.1 指标三项约定（IoU 聚合方式、有效体素掩码定义、Free 记入口径） | 需补充 |
| W9 | P2 | 0.1 m 数据集与 AgriOcc-Dataset 的关系及场景重叠 | 需补充 |
| W10 | P2 | 数据与代码可得性缺获取链接与许可证 | 需补充 |

### 评审者结论（原话摘录）

> "本地可修复的问题基本清零。W1–W5 全部是作者侧证据缺口，其中 W1 是唯一的诚信风险点，
> W2/W3 是任何 top venue 都会追问的点。若 W1 核实无误且 W2/W3 补齐，
> 该稿达到可投稿水平（约 7.5–8/10）。当前状态：almost，不建议在 W1 未核实前投稿。"

### 交付物清单

- `paper_cn/main.tex` — 经 2 轮修改（Round 2 后未再改动）
- `paper_cn/review-stage/AUTO_REVIEW.md` — 本累积日志（含 Round 1/2 完整原始意见与终评）
- `paper_cn/review-stage/REVIEW_STATE.json` — 状态置为 `completed`
- `paper_cn/main.pdf` — 17 页，Round 2 末尾编译产物

---



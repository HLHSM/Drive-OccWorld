\section{实验}\label{sec:experiments}

\subsection{数据集与评价协议}\label{sec:data}
\paragraph{AgriOcc-Dataset}
我们的绝大部分实验在AgriOcc-Dataset上完成，其构建过程见第\ref{sec:dataset_construction}节。
AgriOcc使用正前、左前和右前三路相机，预测机器人前方20m*20m区域的占用。

\paragraph{ORAD-3D}
另外，为测试仿真到真实环境的迁移，我们在\ref{sec:sim2real}节使用 ORAD-3D 数据集\cite{chen2025orad3d}评估AgriOcc在真实野外和农田占用预测任务中的性能。

\paragraph{指标}
我们用mIoU和每个类别的IoU来评价语义占用模型的性能。对类别 $c$，$\IoU_c=\mathrm{TP}_c/(\mathrm{TP}_c+\mathrm{FP}_c+\mathrm{FN}_c)$；
mIoU 为有效类别 IoU 的算术均值。

\subsection{实现细节}\label{sec:implementation}
AgriOcc采用预训练的 ResNet-101为图像编码器，并使用6 层 BEV encoder 与 3 层 occupancy decoder。
输入图像尺寸为 $512\times288$；
体素范围为 $[0,20]\times[-10,10]\times[-2,3]$ m，分辨率 $0.2$ m。
优化器为 AdamW，初始学习率 $2\times10^{-4}$，权重衰减 0.01，余弦学习率策略，
500 iteration 线性 warm-up，batch size为24。使用 4 张 RTX 4090训练 8 epoch。

\subsection{与现有方法的比较}\label{sec:comparison}
表\ref{tab:main}汇总 AgriOcc-Dataset 验证集上的结果。
AgriOcc 达到 56.99\% mIoU，六类 IoU 分别为 91.11\%、28.82\%、74.03\%、68.27\%、43.57\% 和 36.15\%。在已完成的对比实验中，AgriOcc 的总体 mIoU 高于 SurroundOcc、SparseOcc、COTR 和 Drive-OccWorld；其增益主要来自 soil\_ground、drivable、other\_vegetation 和 other\_obstacle。作物 IoU 仍是最具挑战性的指标，反映了株间分离与遮挡建模的困难。

\begin{table}[t]
\centering
\caption{AgriOcc-Dataset 验证集上的三维语义占用结果（\%）。参数量为训练时的可训练参数量。}
\label{tab:main}
\scriptsize
\begin{threeparttable}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lrrrrrrrr}
\toprule
方法 & 参数(M) & mIoU & Free & Crop & Soil & Drivable & Other veg. & Other obs. \\
\midrule
SurroundOcc & 222.38 & 40.33 & 88.95 & 20.20 & 50.44 & 38.92 & 26.79 & 16.69 \\
SparseOcc & 58.14 & 16.50 & 73.53 & 4.66 & 8.99 & 4.02 & 5.82 & 2.00 \\
COTR & 37.17 & 36.50 & 91.11 & 8.59 & 54.48 & 30.88 & 26.50 & 7.42 \\
Drive-OccWorld & 60.68 & 35.15 & 85.52 & 19.63 & 40.29 & 29.64 & 21.06 & 14.77 \\
\midrule
\textbf{AgriOcc} & \textbf{55.74} & \textbf{56.99} & \textbf{91.11} & \textbf{28.82} & \textbf{74.03} & \textbf{68.27} & \textbf{43.57} & \textbf{36.15} \\
\bottomrule
\end{tabular}
}
\end{threeparttable}
\end{table}

\subsection{模块消融}\label{sec:ablation}
表\ref{tab:ablation}汇总已完成的模块配置。Base 的 mIoU 为 53.89\%，单独启用 GVAD 和 Agri-AMoE 后分别达到 56.24\% 和 56.37\%；在二者基础上加入 NearFar 的完整组合达到 56.99\%。NearFar 不增加可训练参数。

\begin{table}[t]
\centering
\caption{AgriOcc-Dataset 模块消融。\checkmark 表示对应模块启用}
\label{tab:ablation}
\small
\begin{tabular}{lccccc}
\toprule
配置 & GVAD & NearFar & Agri-AMoE & 参数(M) & mIoU(\%) \\
\midrule
Base  &  &  &  & 52.12 & 53.89 \\
+ GVAD & \checkmark &  &  & 55.08 & 56.24 \\
+ NearFar & \checkmark & \checkmark & \checkmark & 55.74 & \textbf{56.99} \\
+ Agri-AMoE &  &  & \checkmark & 54.15 & 56.37 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{仿真到真实越野场景迁移}\label{sec:sim2real}

ORAD-3D 的占用输出为 $100\times100\times16$ 的九类体素，类别定义沿用其官方标注协议；坐标轴由原始 right/forward/up 转换为本文的 forward/right/up。AgriOcc-Dataset 与 ORAD-3D 的类别和相机数不同，因而先将 AgriOcc-Dataset checkpoint 中形状兼容的张量加载到 ORAD 模型；三相机 embedding 取中心前视 embedding，新的单目 embedding 和九类占用头由固定随机种子初始化，再对 ORAD 训练集的不同数据比例微调。
表\ref{tab:orad}同时给出 ORAD-3D 官方 \texttt{test} 集和固定 \texttt{test\_farm} 子集的结果。五个 checkpoint 在两套测试清单上独立评估；\texttt{test\_farm} 使用相同的 1,710 帧，mIoU 均按各测试清单中预测或标注出现的语义类计算。AgriOcc-Dataset 初始化在 50\% ORAD 数据微调时于 \texttt{test\_farm} 达到 39.45\% mIoU，而 100\% 数据微调时在官方 \texttt{test} 上达到 20.11\% mIoU。

\begin{table}[t]
\centering
\caption{ORAD-3D 仿真到真实迁移结果（\%）。\texttt{test} 为官方测试集，\texttt{test\_farm} 为测试集中筛选出的农田场景子集。}
\label{tab:orad}
\scriptsize
\begin{tabular}{lrrr}
\toprule
训练方式 & 比例 & Test & Test\_farm \\
\midrule
从头训练 & 100\% & 18.16 & 31.83 \\
\midrule
换预测头微调 & 10\% & 14.33 & 22.29 \\
换预测头微调 & 25\% & 14.96 & 28.64 \\
换预测头微调 & 50\% & 15.70 & \textbf{39.45} \\
换预测头微调 & 100\% & \textbf{20.11} & 26.52 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{体素分辨率迁移}\label{sec:resolution_transfer}
农业机器人在垄间通行、株间避障和精细作业时，$0.2\times0.2\times0.2\,$m 的体素可能将相邻作物、窄垄沟或局部可通行边界量化到同一网格中。将整个 BEV 编码器直接扩展到 $0.1\times0.1\times0.2\,$m 会使 XY 查询数从 $100\times100$ 增至 $200\times200$，即增加四倍；相应地，多尺度图像--BEV 交互、BEV 中间激活和三维占用监督的计算与显存开销均显著上升。为此，本文进一步验证 AgriOcc 是否能够将已学习的粗分辨率三维先验高效迁移到细分辨率占用预测。

具体地，迁移模型首先在 $0.2\times0.2\times0.2\,$m AgriOcc-Dataset 数据上完成训练，保留其 $100\times100$ GVAD--Agri-AMoE--NearFar BEV 编码器；随后在 $0.1\times0.1\times0.2\,$m 数据上微调，并在占用输出端加入学习式 $2\times$ 细化头。该头将粗占用 logits 与相应的 BEV 特征投影融合，输出 $200\times200\times25$ 体素。对照组采用相同的 0.1 m 训练/验证划分、图像输入和 4 epoch 训练时长，但从公开二维图像预训练初始化，直接学习 $200\times200$ 的高分辨率 BEV 与占用预测。两组结果如表\ref{tab:resolution_transfer}所示。

\begin{table}[t]
\centering
\caption{0.1 m AgriOcc-Dataset 验证集上的体素分辨率迁移结果（\%）。迁移方法先在 $0.2\times0.2\times0.2\,$m 数据上训练，再适配至 $0.1\times0.1\times0.2\,$m；两行均训练 4 epoch，使用同一 1:1 序列级训练/验证划分。}
\label{tab:resolution_transfer}
\scriptsize
\resizebox{\linewidth}{!}{%
\begin{tabular}{lrrrrrrrr}
\toprule
方法 & mIoU & Free & Crop & Soil & Drivable & Other veg. & Other obs. \\
\midrule
0.1 m 从头训练 & 36.18 & 89.27 & 16.74 & 52.92 & 20.11 & 23.45 & 14.56 \\
\midrule
\textbf{0.2 m 预训练 $\rightarrow$ 0.1 m 输出细化迁移} & \textbf{49.01} & \textbf{92.39} & \textbf{22.61} & \textbf{73.75} & \textbf{44.29} & \textbf{35.49} & \textbf{25.50} \\
\bottomrule
\end{tabular}
}
\end{table}

分辨率迁移将 mIoU 从 36.18\% 提升至 49.01\%，绝对提高 12.83 个百分点；六类 IoU 均有提升，其中 soil\_ground 和 drivable 分别提高 20.83 和 24.18 个百分点。该结果表明，$0.2\,$m 预训练并未只学习粗糙的类别模板，而是已在图像到 BEV 的投影、可见性约束和场景级语义布局中建立了可复用的三维表征。微调阶段无需从有限的高分辨率数据中重新估计相机几何和大范围田间结构，而是将学习能力集中于细网格上的局部边界、垄沟与作物间隙恢复。

从优化角度看，直接高分辨率训练必须同时学习四倍数量的 BEV 查询及其图像对应关系；在训练样本规模不变时，每个高分辨率体素获得的有效几何观测更稀疏，细粒度 crop--free 边界还受到显著类别不平衡的影响。迁移方案保留 $100\times100$ 编码器的稳定全局上下文，并以残差式 $2\times$ 输出细化将粗预测上采样到 0.1 m 网格。因而，细化头只需学习粗网格未表达的高频空间残差，而不必重建完整的三维场景；这解释了其在作物、地面和可通行区域上的一致增益，也说明 AgriOcc 能在不承担端到端高分辨率 BEV 编码成本的条件下适配精细农业感知需求。

\subsection{讨论}\label{sec:discussion}
AgriOcc 的总体增益集中于地面、可通行区域和复杂背景类别，说明可见性建模、三维结构自适应解码和近远场计算分配能够提升农业场景的大范围空间一致性。作物类别仍是最具挑战性的部分：相邻植株和叶片间隙在单帧三相机投影及 $0.2\,$m 体素化后易发生混合。Agri-AMoE 通过梯度能量在平面局部、垂直和大范围上下文专家之间逐体素路由；完整组合的 crop IoU 为 28.82\%，因此仍需通过多随机种子和补全累积组合消融检验其与 GVAD、NearFar 的交互。NearFar 将近场分辨率优先级显式编码到查询布局中，为农业机器人部署中的精细感知与高效计算提供了统一设计。
